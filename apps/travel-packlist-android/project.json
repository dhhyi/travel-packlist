{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": [
    "travel-packlist-web",
    "webapp-plugin",
    "capacitor-assets"
  ],
  "name": "travel-packlist-android",
  "projectType": "application",
  "tags": [
    "assembly"
  ],
  "targets": {
    "build": {
      "cache": true,
      "configurations": {
        "ci": {
          "bundles": [
            "AAB",
            "APK"
          ]
        }
      },
      "dependsOn": [
        "travel-packlist:build-required"
      ],
      "executor": "@travel-packlist/webapp-plugin:android",
      "inputs": [
        "^production",
        "version"
      ],
      "options": {
        "assemblyTask": "travel-packlist-web:build:android",
        "assetTask": "generate-assets",
        "outputPath": "dist/android"
      },
      "outputs": [
        "{options.outputPath}"
      ]
    },
    "generate-assets": {
      "executor": "@travel-packlist/capacitor-assets:run",
      "options": {
        "type": "android"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cspell lint {projectRoot}/whatsnew/whatsnew-*"
      }
    },
    "serve": {
      "dependsOn": [
        "build"
      ],
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cap run android"
        ]
      }
    }
  }
}
