{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": [
    "check-plugin"
  ],
  "name": "rules",
  "projectType": "library",
  "sourceRoot": "{projectRoot}/src",
  "tags": [
    "scope:state"
  ],
  "targets": {
    "generate": {
      "cache": true,
      "dependsOn": [
        "peggy"
      ],
      "executor": "nx:run-commands",
      "inputs": [
        "{projectRoot}/src/index.d.ts",
        "{projectRoot}/src/**/*.peggy",
        "{projectRoot}/src/**/types.ts"
      ],
      "options": {
        "commands": [
          "sed -i \"1iexport * from './types';\" {projectRoot}/src/index.d.ts",
          "sed -i \"1iimport { Condition, Item, Question, Rule, Rules } from './types';\" {projectRoot}/src/index.d.ts"
        ],
        "parallel": false
      },
      "outputs": [
        "{projectRoot}/src/index.d.ts"
      ]
    },
    "peggy": {
      "cache": true,
      "executor": "nx:run-commands",
      "inputs": [
        "{projectRoot}/peggy.config.cjs",
        "{projectRoot}/src/**/types.ts",
        "{projectRoot}/src/**/*.peggy"
      ],
      "options": {
        "command": "peggy -c peggy.config.cjs",
        "cwd": "{projectRoot}"
      },
      "outputs": [
        "{projectRoot}/src/index.*"
      ]
    }
  }
}
