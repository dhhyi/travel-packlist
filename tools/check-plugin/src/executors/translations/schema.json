{
  "$schema": "https://json-schema.org/schema",
  "additionalProperties": false,
  "description": "",
  "properties": {
    "defaultLocale": {
      "description": "The default locale to use for the translations",
      "type": "string"
    },
    "interactive": {
      "default": false,
      "description": "Whether to run the executor in interactive mode",
      "type": "boolean"
    },
    "pattern": {
      "description": "The pattern to match the files to check",
      "type": "string"
    }
  },
  "required": [
    "pattern",
    "defaultLocale"
  ],
  "title": "Executor Schema",
  "type": "object",
  "version": 2
}
