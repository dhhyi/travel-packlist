{
  "$schema": "https://json-schema.org/schema",
  "title": "Generator Schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The source root of the project"
    },
    "pattern": {
      "type": "string",
      "description": "The pattern matching transformer files"
    }
  },
  "required": [
    "path",
    "pattern"
  ],
  "additionalProperties": false
}
